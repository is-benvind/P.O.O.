const num = "10";
const operacao = num + 10; //1010

// No exemplo acima, o programa, feito em JavaScript (uma linguagem dinamicamente tipada), fez uma conversão 
// implícita do valor 10, de número para string, para então concatenar com a string "10", o que gerou um resultado 
// errado para a soma. 